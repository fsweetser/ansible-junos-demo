if [ -e $HOME/ansible/hacking/env-setup ] ; then source $HOME/ansible/hacking/env-setup ; fi
export PROJECT_HOME=`pwd`
export ANSIBLE_LIBRARY=${PROJECT_HOME}/library:/usr/share/ansible/$([ -n "$ANSIBLE_LIBRARY" ] && echo ":${ANSIBLE_LIBRARY}")
